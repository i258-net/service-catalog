{
  "name": "bones-monorepo",
  "private": true,
  "version": "0.1.0",
  "description": "Git-backed software catalog with CLI and graph browser",
  "license": "MIT",
  "engines": {
    "node": ">=22.18"
  },
  "packageManager": "pnpm@10.30.3",
  "scripts": {
    "build": "pnpm --filter @bones/core build && pnpm --filter @bones/cli build && pnpm --filter @bones/web build",
    "test": "pnpm --filter @bones/core test",
    "bones": "pnpm --filter @bones/core build && node packages/cli/src/cli.ts",
    "dev": "pnpm --filter @bones/core build && pnpm --filter @bones/web dev"
  }
}
